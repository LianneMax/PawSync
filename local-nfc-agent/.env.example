# ─── Required ─────────────────────────────────────────────────────────────────

# Full URL of your Render backend (no trailing slash)
BACKEND_URL=https://pawsync-backend.onrender.com

# Shared secret — must match NFC_SECRET on the Render backend exactly
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NFC_SECRET=change-me-to-a-long-random-secret

# ─── Optional tuning ──────────────────────────────────────────────────────────

# How often the agent polls for pending write commands (milliseconds)
POLL_INTERVAL_MS=3000

# How often the agent tries to drain the offline event queue (milliseconds)
DRAIN_INTERVAL_MS=30000

# winston log level: error | warn | info | debug
LOG_LEVEL=info
